{
  "name": "webux",
  "version": "0.1.0",
  "private": true,
  "description": "A customizable web UX CMS with page builder frontend and backend API.",
  "type": "module",
  "scripts": {
    "dev": "node --watch server/index.js",
    "start": "node server/index.js",
    "check": "node --check server/index.js && node --check server/store.js && node --check public/app.js"
  }
}
